Show list of all available /rp commands:

```
=== RP COMMANDS ===

SESSION:
  /rp              — Start new session (asks setup questions)
  /rp:load [name]  — Load saved session
  /rp:save         — Save current session
  /rp:saves        — List all saved sessions
  /rp:save-delete  — Delete a saved session

INFO (no time passes):
  /rp:sheet        — Character sheet (stats, inventory, party, position)
  /rp:npc          — All known NPCs (allies, enemies, dead)
  /rp:factions     — All factions with clocks
  /rp:chapter      — Current mission progress
  /rp:recap        — Brief session summary
  /rp:map          — Known locations and travel options

META:
  /rp:gm [topic]   — Talk to GM outside roleplay (rules, lore, requests)
  /rp:help         — This list
===
```

---
META: This is a reference command. Does not affect game.

---
> **LANGUAGE:** Preferred session language is **Russian**. This file is stored in English for token efficiency only.
